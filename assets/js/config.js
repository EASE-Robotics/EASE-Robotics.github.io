// ─── Google Form links ──────────────────────────────────────────────────────────
// Paste each Google Form's share link here (Send → link icon → Copy).
// While a link is empty, its buttons show "Form opens soon" and can't be clicked.

window.EASE_FORMS = {
  registerTeam: "", // team registration: team name, school, grade level, members, contact
  requestKit: "",   // free starter kit request + shipping address
  submitVideo: "",  // competition video URL + short description
};
